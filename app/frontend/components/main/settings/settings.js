'use strict';
angular.module('settings', ['ngMaterial', 'hardware', 'environment'])
.component('settings', {
    templateUrl: '/frontend/components/main/settings/settings.html',
    controller: function () {
        self.$onInit = function () {
        };
    }
});
